# 重構過程發現的不合理項目與標準化建議（與環工部討論用）

> 2026-07-11 彙整。來源：Phase 1 legacy 精讀、Phase A Schema B 重構、A→B 搬遷實測、
> V2 UI 改版全程發現。分四類：
> **A＝已在 B 棧標準化（請環工部認可新做法）**、**B＝資料清理（正式搬遷前必做）**、
> **C＝業務規則待確認（規則本身要環工部拍板）**、**D＝權限與流程（等 AD 串接後接上）**。
> 每項標注出處，細節可回查對應文件/程式註解。

## A. 已在 B 棧標準化——請環工部「認可新做法」（新資料建置也照此標準）

| # | 舊系統做法（不合理處） | B 棧新做法 | 討論點 |
|---|---|---|---|
| A1 | **隔離竄改讀值**：隔離核准後直接把 rvalue/管制值覆寫成「保養中」字樣，原始讀值永久遺失 | 讀值照實記錄，保養狀態由隔離單有效區間即時 JOIN 推導，永不覆寫 | 認可即可。注意：舊系統歷史資料已被污染救不回，搬遷時這些區間讀值會標為 maintenance |
| A2 | **讀值混型字串**：rvalue 一欄混存數字/'N.D'/'<0.05'/'斷訊'/'保養中'，每次用都要拆字串猜型別 | 拆成 value(數值)+status(狀態碼)+raw_text(原文備查) 三欄 | 認可即可。新資料建置（QA 輸入、JOB 轉拋）一律照此格式 |
| A3 | **門檻存字串**：'6-9'/'80'/'-'/'建置中' 混存一欄，計算前都要 parse | 拆 low/high 數值 + status(valid/na/building) | 認可即可 |
| A4 | **派報紀錄用字串比對**：msg1/msg2 長字串 LIKE 比對決定首發/再發 | mail_log(信件)+mail_log_item(逐項目+條件碼)正規化 | 認可即可 |
| A5 | **plantid=29 魔法數字**代表「全廠」，程式到處寫死判斷 | plant.kind 欄位（normal/virtual_group/all）明確表達 | 認可即可；新廠區清單請直接標 kind |
| A6 | **歷史讀值無門檻快照**：異常回溯時查不到「當時的管制值是多少」 | reading_history 每筆快照當下 SPEC 門檻（稽核需求，已依你先前要求設計） | 認可即可 |
| A7 | **簡訊/手機欄位**：功能早已停用，欄位一直留著 | B 棧不建這些欄位，新 UI 不顯示 | 認可即可 |
| A8 | 舊 JOB 用「tag 順序相符」逐筆比對，順序一亂就**靜默漏同步**（pH 改名事件根因） | tag_mapping 表明確驅動，每筆對不上都記入 errors 不靜默 | 認可即可；tag 對應表就是你要跟環工部確認的「新資料建置需求」第 4 張表 |

## B. 資料清理——正式搬遷（Phase C）前必須處理的舊資料品質問題

| # | 問題 | 發現場景 | 建議 |
|---|---|---|---|
| B1 | **VOC_plant 有 plantno 重複/空白**（不同 plantid 共用同一代號） | 搬遷實測撞 UNIQUE | 環工部確認每個 plantid 的正名，重複的擇一保留或改名 |
| B2 | **VOC_item 同名不同 id**（如多個 VOC 變體） | 搬遷實測撞 UNIQUE | 項目正名清單（新資料建置需求第 2 張表就是在解這題） |
| B3 | **孤兒參照**：dept/spec 指向不存在的廠區/項目 | 搬遷實測撞 FK | 清單化後由環工部判定刪除或補主檔 |
| B4 | **部門名稱無來源**：系統裡「部門名稱」實際上等於部門代碼 | 部門權限頁盤點 | 若要顯示中文部門名，employee 同步來源需補部門名稱欄位 |
| B5 | 被 A1 污染的歷史讀值區間 | 搬遷設計 | 搬遷時標 maintenance；無法還原原始值，需環工部知情 |

## C. 業務規則待確認——規則本身請環工部拍板（多數已忠實還原舊行為，但舊行為可疑）

| # | 規則 | 現況 | 要拍板的問題 |
|---|---|---|---|
| C1 | ~~雙邊規格只比上界~~ | ✅ **2026-07-31 確認：pH 過低也要亮燈**。已實作：`services/dashboard_service._calculate_light()` 新增 `check_lower_bound` 參數（**預設 False，A 棧行為完全不變**，避免影響公司平行測試中的 A 棧），B 棧呼叫端傳 True 啟用上下界都檢查。單邊規格不受影響。 | ✅ 已實作(2026-07-31) |
| C2 | ~~水質異常通知固定查 K14B~~ | ✅ 環工部確認：維持原行為（K14B 是匯集其他廠棟已處理水的放流點，非 bug）。 | 已結案 |
| C3 | ~~異常報表母集合（含雨水溝）~~ | ✅ **2026-07-31 確認：採新版口徑**（含雨水溝）。與舊系統統計數字的差異視為已知、可接受的行為變更，不需要為了對齊舊數字而改寫。 | 已結案 |
| C4 | ~~隔離重複防呆（區間重疊 vs 完全相等）~~ | ✅ **2026-07-31 確認：採新版（區間重疊即擋）**。較嚴格的防呆是有意選擇，不回退成舊版「完全相等才擋」。 | 已結案 |
| C5 | ~~ControlTime 後門（roleid=12 不走簽核、無時長上限）~~ | ✅ **2026-07-31 決策：採方案 2——維持不走簽核，但時長上限改為可設定**。roleid=12＝「隔離時間修改」是舊系統**專為此功能命名的窄權限**（環工部持有），非意外漏洞，故保留這條營運彈性通道。實作：新增 `system_config['control_time_max_hours']`，量的是**隔離總時長**（新 etime − 該單 stime，與一般申請的 1 小時上限同一量法，只是本通道可另設較寬鬆值）。**預設 0 = 無上限**（維持現行行為，本次是「加開關」非「加限制」，上線不改變任何人現有操作習慣）；驗證放 service 層（pydantic 連不到 DB，「不可縮短」仍由既有 pydantic validator 負責）。僅 B 棧。 | ✅ 已實作(2026-07-31) |
| C6 | ~~規格 OOC/Alert 雙邊寬窄方向~~ | ✅ **2026-07-31 確認：現行「直覺方向」正確**。使用者確認公司內控邏輯是 Alert 最先觸發（範圍最窄，包在 OOC 內）、OOC 次之、OOS 最嚴重（範圍最寬）。已用種子資料 pH1 實例核對（OOS 6-9／OOC 6.5-8.5／Alert 6.8-8.2）與現行 `schemas/spec_schema.py::check_ooc_alert_hierarchy()` 邏輯一致。<br>**2026-07-31 追加確認兩點（已實作）**：<br>① **Alert 與 OOC 允許「貼齊」（相同數值）**——實際數值因廠區而異，部分廠區不留黃燈緩衝、Alert 直接等於 OOC，屬合法設定。原本的嚴格不等式會擋掉這種資料，已放寬成 `>=`／`<=`（`check_ooc_alert_hierarchy()`：單邊 `ooc < alert` 才擋、雙邊 OOC 範圍比 Alert 窄才擋），訊息同步改為「不可小於（可相等）」。**真正的反轉仍擋**：OOC 比 Alert 窄時 `_calculate_light()` 的黃燈條件 `alert < rvalue < ooc` 是空區間，Alert 永遠不會觸發，屬必須擋下的資料錯誤。<br>② **讀值同時碰到 Alert 與 OOC 時以 OOC（橙燈）優先**——查證 `services/dashboard_service._calculate_light()` **runtime 已經正確**（OOC 分支 `ooc <= rvalue < oos` 含等號且排在 Alert 之前），**未改任何 runtime 邏輯**，改以新增 `tests/test_light_ooc_priority.py`（含單邊貼齊、單邊等於 OOC、Alert/OOC 之間、pH 雙邊下界貼齊 `check_lower_bound=True` 等案例）把規則鎖住，防止日後重構燈號順序時破壞。<br>連帶：`scripts/load_user_data.py` 新增「門檻階梯完整性」**警告**（填了 OOC 沒填 OOS → 橙燈不會亮；填了 Alert 沒填 OOC → 黃燈不會亮），警告不擋載入、不影響離開碼。 | 已結案 |
| C7 | ~~改排水原因清單無正式主檔~~ | ✅ **2026-07-31 確認：依主控建議**（先維持 system_config 字串簡化實作，不建正式主檔；日後有需要再升級）。 | 已結案 |
| C8 | ~~異常回覆掛信件層級~~ | ✅ **2026-07-31 確認：改成逐項目回覆**。**這是 schema 調整**——`mail_log_item` 需加回覆欄位（目前回覆掛在 `mail_log` 信件層級），連動 history_service/reason 相關頁面與 API。影響範圍：異常查詢/異常回覆頁面、`/history/reply` API、V2 合併版 modal（history.html）。 | ✅ 已實作(2026-07-31) |
| C9 | ~~隔離時間上限 1 小時~~ | ✅ **2026-07-31 確認：與舊版相同**，維持現行 1 小時上限，不分項目/廠區。 | 已結案 |
| C10 | ~~CheckMAILlog 原始碼未取得~~ | ✅ **2026-07-31 已取得並確認結案**：使用者提供 `ASEJOBS` repo 的 `jobs/SendMail.cs`。`CheckMAILlog`/`CheckMAILlog1` 是 `dbPMS`（電力管理系統）類別的方法，供 `SendMail_IH主機連線異常通知()`/`SendMail_IH_TAG斷訊通知()` 做「今天是否已寄過同一則錯誤」的寄信防重複判斷（回傳 1=已寄過、0=未寄）。這是 **IH(iHistorian) 基礎設施監控的一部分，操作 `dbPMS` 資料庫，與 VOC 水質平台（`dbVOC`）完全無關**，不在本次移植範圍內，**不需要任何動作**。附帶價值：同一份檔案內的 `SendMail_廠務法規許可值標準化管控報表()` 才是真正的 VOC 派報 JOB（`dbVOC`/`GetDataRed`/`GetMailList`/msg1·msg2/K14B CC 邏輯），與現有 `services_b/dispatch_service.py` 的移植邏輯完全吻合，等於一次額外交叉驗證，確認派報邏輯移植正確、無需修改。 | 已結案（無動作） |

## D. 權限與流程——等 AD 串接後一併接上（目前刻意未上鎖）

| # | 項目 | 現況 | AD 後動作 |
|---|---|---|---|
| D1 | **check_permission 未掛守門**：除 ControlTime 外所有端點無權限檢查 | current_user 是 mock，掛了沒意義 | LDAP 有真身分後全面掛上（ACL_ENFORCE=True） |
| D2 | **規格簽核無身分檢查**：待簽核頁任何人可核可否；也沒真正建簽核流程/指派簽核人 | 已知缺口（UI 已預留） | 比照隔離簽核補流程＋身分檢查 |
| D3 | **規格「就地編輯 vs 送簽」無權限區分**：直接編輯立即生效，任何人可用 | 未實作 | 定義誰能直改（管理員？）誰必須走簽核 |
| D4 | **ACL 只管 roleid 3/7**，且後端未真擋其他 role_id（只有前端下拉限制） | ✅ **2026-07-31 使用者提供完整 12 種角色主檔**（見下表），舊系統同樣只管 3/7 兩種 | 是否開放管理全部 12 種角色＋後端補驗證（現在有完整角色名稱可以逐一討論） |
| D5 | **4 頁 schema 驗證未接線**（派送名單/部門/ACL/異常回覆）：格式驗證只存在文件，後端幾乎照單全收 | 盤點發現 | 標準化補接（獨立小工作包，不必等 AD） |
| D7 | **項目類型判斷仍靠名稱字串比對**：`services/dispatch_service.py`（`"VOC" in item`／`"雨水溝" in item`）、`services/flow_service.py`（簽核 rtype 水/空保養中）都是用項目名稱猜類別——新廠若有不叫 VOC 的空汙項目會被誤判成水質，連帶找錯簽核人、派報分錯類 | ✅ 2026-07-31 已加 `item.category` 欄位（水質/空汙/雨水溝），基礎資料載入器已落地正確值，**資料已就位**。**同類問題的「規格型態」已於 2026-07-31 整條做完（見 D8），可作為 D7 讀取端改造的範本** | **讀取端仍未改**：`services/dispatch_service.py` 與 `services/flow_service.py` 判斷邏輯改讀 `item.category` 取代字串比對（會動到 A/B 共用純函式，需比照 C1／D8 用「預設 None＝退回舊推測」的參數保護 A 棧） |
| D8 | ~~**雙邊規格判定靠名稱字串比對**~~：原本兩處實作互不一致（`scripts/load_user_data.py` 的 `^ph\d*$` vs `schemas/spec_schema.py` 的 `item=='pH' or 'pH1'`，`routers_b/spec_router_b.py` 還有第三套 `"pH" in item`），導致 ①`pH2` 灌得進去卻在規格頁改不動、②**溫度（K21）這種雙邊項目根本建不起來**（名稱沒有 pH 字樣，一律判成單邊，填 `20-35` 必被擋） | ✅ **2026-07-31 已改為資料驅動並結案**：新增 `models_b.Item.is_dual_bound`（NULL=未指定→退回名稱推測）；主表新增選填欄「規格型態（單邊/雙邊）」；`SpecBase` 新增可注入的 `is_dual_bound`，B 棧 `routers_b/spec_router_b.py` 先查 `Item.is_dual_bound` 再驗證；基礎資料維護頁可設定。**A 棧（`routers/spec_router.py`／`services/spec_service.py`／`models/`）完全未動**，fallback 凍結為原本的 `item.lower()=='ph' or item=='pH1'` | 已結案。剩餘技術債：A 棧 fallback 與載入器推測的寬窄仍不同（刻意，A 棧凍結）；等 A 棧退場後可一併刪除 `guess_dual_bound_by_name()` |
| D6 | **正式上線設定**：ACL_ENFORCE=True、AUTH_MOCK=False、ALWAYS_CC_EMAIL 清空、TEST_MODE=False | .env.example 已註明 | 上線檢查表項目 |

### 舊系統 12 種角色主檔（2026-07-31 使用者提供，供 D2~D6 討論參考）

| roleid | rolename | B 棧現況 |
|---|---|---|
| 1 | 系統管理員 | 未對應（`is_admin()` 目前另有邏輯，未必等於此角色） |
| 2 | 法規許可值與規格值維護 | 對應規格維護（就地編輯／送簽），見 D3 |
| 3 | 廠區項目隔離抑制維護 | ✅ 已管理（隔離申請權限） |
| 4 | 異常派報簡訊啟用 | 簡訊功能已停用（B 棧決策），此角色語意作廢 |
| 5 | 異常派報簡訊停用 | 同上，作廢 |
| 6 | 派送名單維護 | 對應派送名單頁，未掛權限檢查（D1/D5） |
| 7 | 廠區項目隔離抑制查詢 | ✅ 已管理（唯讀查詢） |
| 8 | 隔離權限維護 | ✅ **2026-07-31 使用者確認：是「修改隔離的資料」的權限，環工部持有**（與 roleid 3「隔離抑制維護」的差別在於這是環工部端的資料修改權，非申請端） |
| 9 | QA手測值更新 | 對應 QA 頁，未掛權限檢查 |
| 10 | 部門權限維護 | 對應部門權限頁，未掛權限檢查 |
| 11 | 中水緊急通知 | 對應中水通知頁，未掛權限檢查 |
| 12 | 隔離時間修改 | ✅ 已管理（`ControlTime`，見 C5——唯一真正掛 `check_permission` 守門的角色） |

有了完整角色名稱，D4「要不要開放管理全部 12 種角色」可以更具體地討論：例如 4/5（簡訊）可以直接排除不做；2/6/9/10/11 都有明確對應的既有頁面，語意清楚，技術上補齊不難；8「隔離權限維護」已確認是環工部端「修改隔離資料」的權限。

## 建議的下一步順序（對應使用者 2026-07-11 提案）

1. **你（使用者）→ 環工部**：帶本文件 C 類（拍板）＋ B 類（資料清理）＋「新資料建置需求」
   4 張表去談。談完的結論回填給主控。
2. **平行進行：AD/LDAP 串接**（使用者提案 1，同意）——這是 D 類全部項目的前置，
   也是「派報/簽核用真人測試」的前置。依 docs/ad_integration_guide.md 實作
   auth_service LDAP 驗證＋session＋current_user 全面取代 MOCK_USER，然後依序接
   D1（守門）→ D2（規格簽核補流程）→ D3/D4。D5 不依賴 AD，可先做。
3. **新 JOB 讀取實際資料**（使用者提案 2，同意）——等環工部給 tag 對應表
   （新資料建置需求第 4 張）後，把 tag_mapping 從 kepware_sim 模擬表換成真 A 端轉拋庫，
   run_sync_worker 對真資料試跑（Track 1 的 write_legacy_sink 也在此階段實測）。
