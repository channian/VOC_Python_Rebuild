# JOB 重構確認清單

> 狀態說明：⬜ 待確認 ／ ✅ 已確認 ／ ❌ 不適用

---

## 一、Tag 命名與廠區對應

| # | 問題 | 為什麼重要 | 狀態 |
|---|------|-----------|------|
| 1 | Kepware Tag 的命名格式是什麼？（例如 `K1.COD`、`Plant_K1/COD`、還是其他？） | 決定如何從 Tag path 解析出 `plantno` + `item` | ⬜ |
| 2 | 新舊 Tag 命名有無對應表？（iFIX Tag name vs Kepware Tag name） | 確認是 1:1 對應還是需要 mapping 表 | ⬜ |
| 3 | 新系統的 `tag_mapping` 表要由誰來維護（手動填、還是 Kepware 可以匯出）？ | 影響 Admin UI 的設計 | ⬜ |

---

## 二、讀值與寫入邏輯

| # | 問題 | 為什麼重要 | 狀態 |
|---|------|-----------|------|
| 4 | 新 JOB 除了讀「即時讀值 rvalue」，有沒有也讀「SCADA 自設的管制值」（OOS_HH / OOC_H / alert）？ | 這是橙燈「設定不一致」的比對來源，缺少這個橙燈邏輯就跑不完整 | ⬜ |
| 5 | CWMS 的管制值（OOS_HH1 / OOC_H1）新 JOB 有沒有對應的來源？ | CWMS 是另一套系統，Kepware 是否有接 CWMS 的點 | ⬜ |
| 6 | 雙邊規格的下限值（OOS_LL / OOC_L / alert_L，pH 專用）新 JOB 有處理嗎？ | pH 有上下限，缺下限判斷燈號會有誤 | ⬜ |
| 7 | 新 JOB 寫入頻率是多少？還是維持每 15 分鐘？ | 影響儀表板更新頻率的設計 | ⬜ |
| 8 | 新 JOB 有沒有同時寫「歷史資料表」（對應舊的 VOC_SCADA_HIST）？ | 雨水溝預警需要讀 T-15min、T-30min 的歷史值 | ⬜ |

---

## 三、斷訊（broken）判斷

| # | 問題 | 為什麼重要 | 狀態 |
|---|------|-----------|------|
| 9 | 新 JOB 怎麼判斷斷訊？用 Kepware Tag 的品質碼（Quality Code）？還是「超過 N 分鐘沒有新值」？ | 斷訊判斷邏輯要在新系統重現，否則畫面上看不出哪個感測器離線 | ⬜ |
| 10 | broken 的狀態值怎麼定義？舊系統是 0=正常、1=斷訊、2=保養中（保養中是 Web 端寫的，不是 JOB） | 新系統要確保 Web 端和 JOB 端的 broken 值不會互相覆蓋 | ⬜ |

---

## 四、雨水溝 24 小時累積值（TwentyFourHours）

| # | 問題 | 為什麼重要 | 狀態 |
|---|------|-----------|------|
| 11 | 舊系統的 `TwentyFourHours` 是從 `PMS.dbo.Water_WindRainHistValue` JOIN 過來的，不是 JOB 算的。**新系統這個值從哪裡來？** | 雨水溝預警的燈號（紅燈條件之一是「連續上升 + 24H 累積=0」）依賴這個值 | ⬜ |
| 12 | PMS 系統會一起遷移到 PostgreSQL 嗎？還是繼續跨 DB JOIN？ | 決定新 Python 系統要怎麼取這個值 | ⬜ |
| 13 | K1 / K9 廠區的雨水溝有特殊的「連續三點上升」邏輯，新系統有沒有保留這兩個廠區的區別？ | 這段邏輯在 SQL 裡寫得很複雜，重構時容易漏掉 | ⬜ |

---

## 五、PostgreSQL Schema（新 DB）

| # | 問題 | 為什麼重要 | 狀態 |
|---|------|-----------|------|
| 14 | 新 JOB 寫入的 PostgreSQL 資料表名稱和欄位名稱是什麼？（請提供 CREATE TABLE 或欄位清單） | 新 Python Web 要改讀 PG 時需要對應 | ⬜ |
| 15 | `light` 欄位（燈號）新 JOB 有沒有繼續寫？（建議新系統不存，改在 Web 計算） | 避免 JOB 和 Web 雙重計算燈號、結果不一致 | ⬜ |
| 16 | `rvalue` 的型別在 PostgreSQL 是 `VARCHAR` 還是 `NUMERIC`？ | 舊系統用 VARCHAR 是因為有 N.D / 斷訊 等文字，新系統要確認處理方式 | ⬜ |

---

## 六、過渡期（雙軌並行）

| # | 問題 | 為什麼重要 | 狀態 |
|---|------|-----------|------|
| 17 | 新 Python Web 在過渡期間讀的是 MSSQL 還是 PostgreSQL？ | 決定 Web 重構的優先序：先接 MSSQL 跑起來，之後再切 PG | ⬜ |
| 18 | 舊 JOB（iFIX → MSSQL）和新 JOB（Kepware → PG）會同時跑嗎？還是切換？ | 影響資料一致性，以及新系統測試期間的資料來源 | ⬜ |

---

*清單建立日期：2026-05-16*
