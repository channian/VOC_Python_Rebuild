# 廠務法規許可標準化管理平台 — 系統架構說明

> 本文件目的：讓接手人員在 15 分鐘內看懂這個系統在做什麼、怎麼運作、哪裡容易出問題。

---

## 一、這個系統在做什麼

廠區的廢水、空汙等環保數據必須符合法規標準（許可值）。這個系統做三件事：

| 功能 | 說明 |
|------|------|
| **即時監控** | 從 SCADA / CWMS 抓最新量測值，跟法規許可值比較，用紅/橙/黃/綠燈顯示狀態 |
| **隔離申請簽核** | 設備維修或異常時，提出隔離申請→主管簽核，期間暫停該項目的異常派報 |
| **異常通報** | 超標時自動發 Email 和簡訊給負責人 |

### 燈號判斷邏輯

```
最新讀值 vs 三層門檻值：

  最新讀值 ≥ OOS (Out of Spec)          → 紅燈 🔴  最嚴重
  OOC ≤ 最新讀值 < OOS                  → 橙燈 🟠
  Alert < 最新讀值 < OOC                 → 黃燈 🟡
  最新讀值 ≤ Alert                       → 綠燈 🟢  正常
  SCADA/CWMS 管制值 ≠ SPEC 設定值        → 橙燈 🟠  (管制值不一致)
```

---

## 二、整體系統架構

```mermaid
graph TB
    subgraph 資料來源["資料來源（唯讀）"]
        SCADA["SCADA 系統\n每 15 分鐘自動更新"]
        CWMS["CWMS 水務管理系統"]
        PMS["PMS 廠務管理系統\n(雨水量)"]
    end

    subgraph DB["資料庫（SQL Server）"]
        VOC_DB["VOC 資料庫\n主業務資料"]
        UTIDB["UTIDB\n員工資料"]
        SignFlow["SignFlow\n簽核流程"]
    end

    subgraph Oracle["Oracle CIM（唯讀）"]
        CIM["工廠 CIM 系統\nfacpdb"]
    end

    subgraph App["ASP.NET 應用程式"]
        dbVOC["dbVOC.cs\n⚠️ 4,931 行 全部商業邏輯都在這裡"]
        Pages["25 個 .aspx 頁面"]
        Auth["Windows 認證\n(AD 網域帳號)"]
    end

    subgraph User["使用者"]
        Browser["瀏覽器"]
    end

    SCADA -->|"每 15 min 寫入"| VOC_DB
    CWMS -->|"同步"| VOC_DB
    PMS -->|"雨量資料"| VOC_DB
    CIM -->|"唯讀查詢"| Oracle
    VOC_DB <--> dbVOC
    UTIDB <--> dbVOC
    SignFlow <--> dbVOC
    Oracle -.->|"Web.config 直連"| dbVOC
    dbVOC <--> Pages
    Auth --> Pages
    Pages <--> Browser
```

---

## 三、頁面地圖與功能對應

```mermaid
graph LR
    Home["🏠 Home.aspx\n主儀表板（燈號總覽）"]

    subgraph 維護功能["維護功能（需權限）"]
        EditSPEC["EditSPEC.aspx\n法規許可值與規格值維護"]
        EditControl["EditControl.aspx\n廠區項目隔離申請"]
        EditMailList["EditMailList.aspx\n派送名單維護"]
        EditAclUser["EditAclUser.aspx\n隔離權限維護"]
        EditQA["EditQA.aspx\nQA 手測值更新"]
        EditDeptList["EditDeptList.aspx\n部門權限維護"]
        VOCreason["VOCreason.aspx\n異常原因回覆"]
        ControlTime["ControlTime.aspx\n隔離時間修改"]
    end

    subgraph 簽核流程["簽核流程"]
        SignControl["SignControl.aspx\n隔離申請簽核"]
        SignSPEC["SignSPEC.aspx\n法規許可值申請簽核"]
        ApplySPEC["ApplySPEC.aspx\n法規許可值申請"]
        ModifyControl["ModifyControl.aspx\n隔離申請修改"]
    end

    subgraph 查詢報表["查詢與報表"]
        MyApply["MyApply.aspx\n個人申請單查詢"]
        PlantApply["PlantApply.aspx\n廠區申請單查詢"]
        VOChistory["VOChistory.aspx\n異常件數查詢"]
        VOCreport["VOCreport.aspx\n異常報表統計"]
    end

    subgraph 特殊監控["特殊監控"]
        RainGutter["RainGutter.aspx\n雨水溝預警"]
        WaterUrgent["WaterUrgent.aspx\n中水緊急通知"]
    end

    Home --> 維護功能
    Home --> 簽核流程
    Home --> 查詢報表
    Home --> 特殊監控
```

---

## 四、資料庫核心資料表

```mermaid
erDiagram
    VOC_plant {
        string plantno PK "廠區代號 (K1/K3...)"
        int plantid "廠區ID"
        string plantno_name "廠區名稱"
        int sort "顯示順序"
        bool isShow "是否顯示"
    }

    VOC_item {
        string item PK "項目代號"
        int itemid "項目ID"
        string unit "單位"
        string stype "類型 (廢水/空汙...)"
    }

    VOC_SPEC {
        string plantno FK "廠區"
        string item FK "項目"
        float LAW "法規許可值"
        float OOS "SPEC Out of Spec"
        float OOC "SPEC Out of Control"
        float alert "預警值"
        float recv "允收值"
        int source "資料來源 (SCADA/CWMS/QA)"
        int status "狀態"
    }

    VOC_SCADA_WEB {
        string plantno FK "廠區"
        string item FK "項目"
        string rvalue "最新讀值"
        float OOS_HH "SCADA 管制上上限"
        float OOC_H "SCADA 管制上限"
        float alert "SCADA 預警值"
        float OOS_HH1 "CWMS 管制上上限"
        float OOC_H1 "CWMS 管制上限"
        datetime cdatetime "資料時間"
        bool broken "斷訊旗標"
    }

    VOC_closectl {
        int seqno PK "申請序號"
        string plantno "廠區"
        string item "項目"
        datetime stime "隔離開始時間"
        datetime etime "隔離結束時間"
        string reason "隔離原因"
        int status "申請狀態"
        string empno "申請人"
    }

    VOC_plant ||--o{ VOC_SPEC : "有"
    VOC_item ||--o{ VOC_SPEC : "有"
    VOC_plant ||--o{ VOC_SCADA_WEB : "量測"
    VOC_item ||--o{ VOC_SCADA_WEB : "量測"
    VOC_plant ||--o{ VOC_closectl : "申請隔離"
```

---

## 五、燈號判斷完整資料流

```mermaid
sequenceDiagram
    participant SCADA
    participant DB as VOC 資料庫
    participant App as Home.aspx.cs
    participant UI as 瀏覽器

    Note over SCADA,DB: 每 15 分鐘自動更新
    SCADA->>DB: 寫入 VOC_SCADA_WEB (最新讀值 + SCADA 管制值)

    Note over UI,App: 使用者進入首頁（或倒數到 0 自動重整）
    UI->>App: GET Home.aspx
    App->>DB: dbVOC.ListVOC() — 撈 VOC_SPEC + VOC_SCADA_WEB
    DB-->>App: 返回每個廠區/項目的讀值與規格值

    loop 每一列資料
        App->>App: 比較 rvalue vs LAW / OOS / OOC / alert
        App->>App: 決定燈號顏色（紅/橙/黃/綠）
        App->>App: 檢查 SCADA 管制值是否與 SPEC 一致
    end

    App-->>UI: 渲染表格（GridView）
    UI-->>UI: JavaScript 倒數計時器<br/>下一個 15 分鐘整點自動重整
```

---

## 六、隔離申請簽核流程

```mermaid
stateDiagram-v2
    [*] --> 草稿
    草稿 --> 待簽核 : 申請人送出\nEditControl.aspx
    待簽核 --> 簽核中 : 第一層主管核准\nSignControl.aspx
    簽核中 --> 生效中 : 最終核准
    生效中 --> 已結束 : 到達結束時間或手動關閉
    待簽核 --> 已退回 : 主管拒絕
    已退回 --> 草稿 : 申請人修改重送\nModifyControl.aspx

    生效中 : 生效中\n(系統暫停該項目異常派報)
```

---

## 七、權限系統說明

```mermaid
graph TD
    WindowsAD["Windows AD\n網域帳號登入"] --> Session["Session 儲存\nSess_UserEmpNo\nSess_UserDept"]

    Session --> Check["dbAclRights.Check權限()"]
    Check --> RoleTable["sys_acluserrole\n員工/部門 → 角色"]
    RoleTable --> RightsTable["sys_aclrolerights\n角色 → 功能權限"]

    RightsTable --> R1["權限 1: 系統管理員"]
    RightsTable --> R2["權限 2: 法規許可值維護"]
    RightsTable --> R3["權限 3: 廠區隔離維護"]
    RightsTable --> R4["權限 4: 異常派報啟用"]
    RightsTable --> R5["權限 5: 異常派報停用"]
    RightsTable --> R6["權限 6: 派送名單維護"]
    RightsTable --> R7["權限 7: 隔離查詢"]
    RightsTable --> R8["權限 8: 隔離權限維護"]
    RightsTable --> R9["權限 9: QA手測值"]
    RightsTable --> R10["權限 10: 部門權限"]
    RightsTable --> R11["權限 11: 中水緊急通知"]
    RightsTable --> R12["權限 12: 隔離時間修改"]

    RightsTable --> PlantFilter["廠區過濾\nplantid=29 → 看全廠\n其他 → 只看被授權的廠區"]
```

### 權限矩陣（主要按鈕可見性）

| 按鈕 | 需要的權限 | 說明 |
|------|-----------|------|
| 法規許可值與規格值維護 | 權限 2 | 修改 LAW/OOS/OOC/Alert 等門檻 |
| 廠區項目隔離抑制維護 | 權限 3 | 新增隔離申請 |
| 異常派報簡訊啟用/停用 | 權限 4 / 5 | 全局開關 |
| 派送名單維護 | 權限 6 | 修改收件人 |
| 隔離權限維護 | 權限 8 | 管理誰能申請哪個廠區 |
| QA手測值更新 | 權限 9 | 手動輸入量測值 |
| 部門權限維護 | 權限 10 | 管理部門可看哪些廠區 |
| 中水緊急通知 | 權限 11 | 發緊急通知 |
| 隔離時間修改 | 權限 12 | 事後修改隔離期間 |

---

## 八、程式碼結構（為什麼難維護）

```
VOC/
├── App_Code/
│   ├── dbVOC.cs          ⚠️ 4,931 行！所有商業邏輯、SQL、Email、SMS 都在這
│   ├── dbAclRights.cs    ✅ 還算獨立（權限查詢）
│   ├── AppConfig.cs      ✅ Session 包裝
│   ├── dbUTIDB.cs        ✅ 員工資料查詢
│   ├── SendSMS.cs        簡訊發送
│   └── BasePage.cs       頁面基礎類別
│
├── [PageName].aspx       25 個 UI 頁面（HTML 混 ASP.NET 控制項）
├── [PageName].aspx.cs    25 個後置代碼（總計 5,851 行）
│
└── Web.config            DB 連線字串、認證設定
```

### 主要痛點

| 問題 | 影響 |
|------|------|
| `dbVOC.cs` 一個檔案包山包海 | 找函式要 Ctrl+F，改一個地方怕影響其他地方 |
| 函式命名混用中文（`List廠區`, `List廠區0`, `List廠區1`...） | 根本不知道哪個給哪個頁面用 |
| SQL 字串拼接直接寫在 C# 裡 | 無法重用、難以測試 |
| 沒有測試 | 改完不知道有沒有壞掉 |
| Windows 認證綁定 AD | 只能在公司內網跑，部署困難 |
| Oracle 連線字串寫死在 `Web.config` | 包含帳號密碼明文（安全風險）|

---

## 九、各廠區代號對照（已知）

| plantno | 說明 |
|---------|------|
| K1, K9 | 有雨水溝特殊監控邏輯的廠區 |
| ALL | 系統管理用（plantid=29 可看全廠） |
| 環工部, GMO | 排除在廠區清單外的特殊節點 |

> ⚠️ 完整廠區清單需查詢資料庫 `VOC_plant` 資料表。

---

## 十、已啟動的 Python 重構專案

學姊已經啟動了現代化重構，放在 `VOC_Python_Rebuild/`：

```
舊系統 (ASP.NET)          新系統 (FastAPI Python)
─────────────────         ─────────────────────────
dbVOC.cs (4931 行)    →  services/ (8 個獨立 Service)
.aspx.cs (5851 行)    →  routers/ (7 個 REST API)
直接 SQL 字串         →  SQLAlchemy ORM models/
Windows 認證          →  LDAP → JWT Token
沒有測試              →  Pytest 10 個測試模組
```

**重構進度**：Phase 1-5 架構已完成，處於雙軌並行測試階段（TEST_MODE 避免二次派報）。

---

## 十一、快速 Troubleshooting

| 現象 | 先檢查的地方 |
|------|------------|
| 儀表板燈號沒更新 | SCADA 是否在寫 `VOC_SCADA_WEB`？查 `cdatetime` 欄位 |
| 某廠區看不到 | 使用者部門 → `VOC_dept` → `plantid` 對應是否正確 |
| 按鈕不見了 | `sys_acluserrole` / `sys_aclrolerights` 是否有對應權限 |
| Email 沒收到 | `VOC_Mail_List` 收件人名單、以及派報是否被停用 |
| 隔離申請送出後沒動靜 | `VOC_closectl.status` 查申請狀態，`SignControl.aspx` 是否有待簽核項目 |

---

*文件產出日期：2026-05-16 | 由 Claude Code 依程式碼逆向分析產生*
